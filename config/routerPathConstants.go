package config

/**
	Router Path Enum
 */

// TODO Collect to route SERVICE VERSION group path
const (
	V1 = "v1"
)

// TODO Collect to route Broadcast Path
const (
	CHANNEL = "/channel"
	PUBLISH = "/publish"
	UNPUBLISH = "/unpublish"
)

// TODO Collect to route Monitoring Path
const (
	JANUSINFO = "/janusinfo"
	NGINXINFO = "/nginxinfo"
	HEALTH    = "/health"
)

